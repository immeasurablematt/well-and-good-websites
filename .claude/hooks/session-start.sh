#!/bin/bash
# Legacy cloud installer retired. Shared skills must be provisioned from the
# managed mb-skills library, following ~/dev/skills/README.md.
# This file is deliberately inert for older checkouts that still reference it.
exit 0
