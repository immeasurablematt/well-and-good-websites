"""Page showpiece artwork for the Well and Good Growth build (empty for now).

Filled by the showpieces agent: the homepage hero bridge (draws itself, then
the span lifts), the homepage service drawings (motion piece 13), the AI
automation canal lock section, the 404 bridge up (piece 16), and the
thank-you last lock (piece 17). Shared geometry, the ship, and the mark come
from growth_art.py. build_growth.py already imports this module as
`showpieces`. Keep this file em-dash free.
"""
