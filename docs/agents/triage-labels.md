# Triage labels

For future issue triage, use these roles: `needs-triage`, `needs-info`, `ready`, `in-progress`, and `blocked`.

Inspect the repository's actual labels before use and map existing equivalents first. This setup does not create or change remote labels. Implementation briefs already approved by Matthew do not need incoming-issue triage.
